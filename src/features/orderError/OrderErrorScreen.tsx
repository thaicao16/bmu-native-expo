import React from 'react';
import { Text, View } from 'react-native';

export const OrderError =() => {
    return(
        <View>
            <Text>Order Error Screen</Text>
        </View>
    )
};
